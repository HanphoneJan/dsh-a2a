# Standalone project: @deepseek-ai/* resolve through tsconfig paths to the
# harness checkout (peer deps provided by the composition at runtime), so
# pnpm must not attempt to auto-install peers from the registry.
auto-install-peers=false